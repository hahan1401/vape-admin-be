services:
  - name: nestjs-app
    type: web
    runtime: node
    plan: free
    buildCommand: 'npm install && npm run build'
    startCommand: 'npm run start:prod'
    autoDeploy: true
    branch: test
